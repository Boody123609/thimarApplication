import 'package:flutter/material.dart';

class ActivationOfAccountPage extends StatelessWidget {
  const ActivationOfAccountPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
